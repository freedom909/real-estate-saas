export const LISTING_TOKENS = {
  ListingRepository: Symbol.for("ListingRepository"),
  // If you were using a specific ORM model, you might register it here too:
  // ListingModel: Symbol.for("ListingModel"),
};